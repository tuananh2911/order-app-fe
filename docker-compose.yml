version: "3.6"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 80:3000
        env_file:
            - ./.env.template
        restart: unless-stopped